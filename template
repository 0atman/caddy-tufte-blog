<!DOCTYPE html>
<html>
    <head>
        <title>{{.Name}}</title>
        <link rel="stylesheet" href="/tufte.css"/>
    </head>
    <body>
        {{if .CanGoUp}}<a href="..">Up one level</a><br>{{end}}
        <h1>{{.Path}}</h1>
        {{range .Items}}
        <a href="{{.URL}}">{{.Name}}</a><br>
        {{end}}
    </body>
</html>
